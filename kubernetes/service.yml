apiVersion: v1
kind: Service
metadata:
  name: book-store-service
spec:
  type: LoadBalancer
  selector:
    app: book-store-deployment
  ports:
    - name: book-store-app-port
      protocol: TCP
      port: 8080
      targetPort: 8080